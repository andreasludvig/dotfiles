# File to be sourced for aliases

# GIT
alias ll='ls -lah'
alias ga='git add'
alias gc='git commit'
alias gp='git push'

# Zsh
alias d='dirs -v'
for index ({1..9}) alias "$index"="cd +${index}"; unset stack
# Can be expanded to whatever number we want.
